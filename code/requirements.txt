# Buy or Wait? — pinned dependencies.
# Install into the repo-root .venv only, never globally:
#   python -m venv .venv
#   .venv/Scripts/python.exe -m pip install -r requirements.txt   # Windows
#   .venv/bin/python -m pip install -r requirements.txt           # macOS / Linux
#
# Note: code/evaluation/main.py (the scorer) uses the standard library only and
# needs none of these. They exist for the model-calling pipeline built later.

# --- direct dependencies ---
openai==1.109.1          # Azure OpenAI client (primary role, per .env)
google-genai==1.39.1     # Gemini client (verifier role — different model family)
python-dotenv==1.1.1     # loads .env; secrets stay out of the repo
pytest==8.4.2            # tests/, must run with no API key present

# --- transitive, pinned for a reproducible resolve ---
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
cffi==2.1.1
charset_normalizer==3.5.1
colorama==0.4.6
cryptography==50.0.1
distro==1.9.0
exceptiongroup==1.3.1
google-auth==2.58.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jiter==0.17.0
packaging==26.3
pluggy==1.6.0
pyasn1==0.6.4
pyasn1-modules==0.4.2
pycparser==3.0
pydantic==2.13.5
pydantic-core==2.46.5
pygments==2.21.0
requests==2.34.2
sniffio==1.3.1
tenacity==9.1.4
tomli==2.4.1
tqdm==4.70.1
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
websockets==15.0.1
